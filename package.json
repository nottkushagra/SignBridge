{
  "name": "signbridge",
  "version": "2.0.0",
  "private": true,
  "description": "SignBridge — Universal Sign Language Assistant",
  "scripts": {
    "dev": "npm --prefix React_files run dev",
    "build": "npm --prefix React_files run build",
    "preview": "npm --prefix React_files run preview",
    "lint": "npm --prefix React_files run lint"
  }
}
